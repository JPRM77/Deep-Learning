# Deep-Learning
A deep dive into neural networks using only Python and NumPy. This repository features manual implementations of backpropagation, optimizers, and architectures to understand the math "under the hood" without high-level frameworks.
